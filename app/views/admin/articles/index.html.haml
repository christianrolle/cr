#user_nav
  - if current_user
    = link_to t('admin.articles.new.title'), new_admin_article_path
    .right
      = link_to "Logout", logout_path
    = render @articles
  - else
    .right
      = link_to "Login", login_path
