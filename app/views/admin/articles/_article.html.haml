%li.alternated.list-group-item.clearfix{ id: dom_id(article) }
  .row
    .col-md-8
      %h1.h3.lobster
        = article.title
      = present(article, ArticlePresenter).tags_with_icon
    .col-md-2
      = article.publishing_date
    .col-md-2.text-right
      = link_to('', article_path(article.slug), rel: 'preview',
          class: 'btn btn-default glyphicon glyphicon-eye-open',
          target: '_blank') if article.published?
      = link_to '', [:edit, :admin, article], 
          class: 'btn btn-default glyphicon glyphicon-pencil'
      = link_to '', [:admin, article], remote: true, method: :destroy, 
          class: 'btn btn-default glyphicon glyphicon-trash'
