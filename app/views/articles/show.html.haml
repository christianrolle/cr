- present(@localized_article, ArticlePresenter) do |article_presenter|
  - content_for :title, @localized_article.title
  - content_for :description, @localized_article.title
  - content_for :meta do
    -#%meta{ property: 'og:locale', content: @localized_article.locale }
    -#%meta{ property: 'og:type', content: 'article' }
    %meta{ property: 'og:title', content: @localized_article.title }
    -#%meta{ property: 'og:headline', content: @localized_article.title }
    %meta{ property: 'og:description', content: @localized_article.summary }
    %meta{ property: 'og:image', content: gravatar_url(200) }
    -#%meta{ property: 'og:url', content: article_presenter.url }
    -#%meta{ property: 'og:site_name', content: 'problems.map(&:solved)' }
    %meta{ property: 'twitter:card', content: 'summary' }
    %meta{ name: 'twitter:site', content: '@chrissonar' }
    -#%meta{ name: 'twitter:title', content: @localized_article.title }
    -#%meta{ name: 'twitter:description', content: @localized_article.summary }
    -#%meta{ name: 'twitter:creator', content: '@chrissonar' }
    -#%meta{ name: 'twitter:image', content: gravatar_url(200) }

  %header
    %time{ datetime: l(@localized_article.published_on) }
      = l @localized_article.published_on, format: :long

  %article
    %h1.lobster= @localized_article.title
    = article_presenter.tags_with_icon
    .text
      = preserve { MarkdownService.call @localized_article.content }

  %footer
    %span= t('.share')
    =# link_to_share_social :twitter, via: 'chrissonar', 
        url: article_presenter.url, text: @localized_article.title
    = link_to_share_social :twitter, via: 'chrissonar', 
        url: "https://www.chrisrolle.com/blog/rendering-collections", 
        text: @localized_article.title

    = link_to_share_social :googleplus, url: article_presenter.url
    = link_to_share_social :facebook, u: article_presenter.url, 
        title: @localized_article.title, summary: @localized_article.summary
