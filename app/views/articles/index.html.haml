%h1 Articles
%ul.inline-list
  - @articles.each do |article|
    %li
      .published_on= I18n.localize article.published_on, format: :short
      %h2
        = link_to article.title, article_path(article.slug)
